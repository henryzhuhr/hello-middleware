services:
  # Redis
  redis-server:
    image: redis:7 # https://hub.docker.com/_/redis
    ports:
      - "6379:6379"
    networks:
      - default
      
# -- 网络名称，用于容器间通信
networks:
  default:
    name: hello-middleware-network

# -- 数据卷名称，用于持久化数据
volumes: